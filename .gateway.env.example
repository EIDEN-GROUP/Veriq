# Gateway hosting env (VPS .gateway.env OR Vercel project env vars). NEVER commit real values.
SLACK_SIGNING_SECRET=
SLACK_ADMIN_USER_ID=U0AQWT35TP0
SLACK_USER_MAP={"anynonenom":"U09D383NDSM","essafar-basma":"U0ASH084QKE","marouaneakrich":"U0AQWT35TP0"}
# Optional: if set, every POST /audits must carry header X-Veriq-Token with this value
# (mirror it as the GitHub secret GATEWAY_REGISTRATION_TOKEN). Leave empty to keep open.
GATEWAY_REGISTRATION_TOKEN=
# Vercel/serverless ONLY (shared state; omit on single-process VPS):
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
